# AXS 设计工作室项目说明与协作规则

> [!IMPORTANT]
> **【双面特工机制 - 外部伪装层】**
> 本文件定义的是 AXS 系统的 **“对外交涉与客户触达”** 准则。
> 在操作根目录及外部前端触达文件时，AI 必须展现为 **“温暖、权威、专业”** 的管家形象。
> 一旦进入 `AXS设计工作室_装修管理系统` 内部执行数据计算和物理图纸生成，请立刻切换为该内部目录下 `AGENTS.md` 规定的 **“极端冷酷无情的特工矩阵”** 状态。

本文件面向后续进入本目录工作的 Codex / Agent。请先阅读本文件，再修改表单、脚本或系统文档。

## 1. 项目定位

AXS 设计工作室是一个面向室内设计与装修管理的本地化工作系统。

核心理念是：

> 所有极致的实用主义与系统化的代码，最终，都是为了毫无痕迹地服务于客户的审美与舒适。

项目不是普通宣传站，也不是单一问卷文件。它包含：

- 客户需求采集 H5 表单。
- 本地知识资产库与设计理念库。
- 装修项目管理、预算、施工验收、复盘沉淀等 SOP。
- 面向后续自动化的 Python 脚本与 JSON/Markdown 数据。
- 用于 AXS 室内设计工作流的专属 Skill 与系统说明文档。

工作时要把它理解为“本地化的设计工作室操作系统”，而不是孤立的前端页面。

## 2. 当前关键文件

根目录常见关键文件如下：

- `AXS_设计工作室客户需求深度调研表.html`
  - 当前浏览器入口文件。
  - 已由 `_最终完美版.html` 恢复为同名入口，避免旧 `file://` 地址失效。
  - 客户填写后可导出 JSON 数据。
- `AXS_设计工作室客户需求深度调研表_最终完美版.html`
  - 当前定稿版本的备份/正式版本。
  - 若入口文件缺失，可从此文件恢复。
- `AXS_设计工作室客户需求深度调研表_极客终极版.html`
  - 另一个更强视觉/极客风格版本，改动前要确认是否真要使用。
- `AXS_设计工作室客户需求深度调研表_纯净文本版.md`
  - 表单内容的纯文本参考。
- `README说明书-AXS设计工作室.md`
  - 系统总纲和商业/工作流定位。
- `系统使用总说明.md`
  - Agentic 闭环、8 大业务流程、SOP、脚本使用说明。
- `AXS系统文档索引目录.md`
  - 文档导航索引。
- `AXS_室内设计专属工作流_Skill.md`
  - 面向需求分析、AI 概念图、AutoLISP 结构图和设计说明的工作流说明。
- `AXS设计工作室_装修管理系统/`
  - 更完整的装修管理系统目录，内部已有自己的 `AGENTS.md`，进入该子目录后还要遵守其中规则。

## 3. 目录结构认知

当前根目录下常见结构：

- `01_知识资产库/`
  - 设计理念、专家 Skill、标准知识沉淀相关资产。
- `05_系统杂项脚本/`
  - 当前主要包含 `axs_survey_fetcher.py` 等辅助脚本。
- `AXS设计工作室_装修管理系统/`
  - 更完整的项目管理系统，包括知识库、前端触达层、项目模板、进行中项目、归档项目和工具脚本。
- 根目录还有大量临时修复脚本、字段 JSON、对比/同步脚本。
  - 不要随意删除或清理这些文件。
  - 需要整理时先向用户确认。

## 4. 当前 H5 表单状态

当前主入口为：

`AXS_设计工作室客户需求深度调研表.html`

表单当前方向：

- 文案已从早期“极客/红线/审判”强压迫表达，调整为更温暖、权威、专业的客户沟通表达。
- 顶部标题为 `AXS 设计工作室`。
- 系统理念文案为温和版：
  - `所有极致的 实用主义 与 系统化的代码， 最终，都是为了毫无痕迹地服务于您的审美与舒适。`
- 不再出现 `Obsidian` 相关按钮文案。
- 不再保留“欧式法式红色警戒线”之类对客户不友好的选项。
- 已加入：
  - 户型图上传。
  - 客户意向设计图上传。
  - 男女主出生日期和出生时间，用于后续风水、易经等传统空间观念参考。
  - 智能系统基础布局。
  - 逐空间规划：主卧、次卧 1、次卧 2、次卧 3、客厅、就餐区、厨房、卫生间、玄关/入户区、阳台、书房、休闲区、储物/家政区。
- `02. 空间规划` 和 `03. 归家动线` 当前应优先保持“选择题 + 其它自填”的形式。

修改表单时注意：

- 保持字段 `name` 稳定，除非同步修改导出逻辑。
- 选择题新增选项时，优先保留 `其它` 自填。
- 文件上传在纯前端导出中通常只能记录文件名，不能自动把图片内容打包进 JSON。
- 如果调整题目编号，要同步检查页面显示顺序。
- 修改后至少执行一次 JavaScript 语法检查。

推荐检查命令：

```powershell
$path='F:\吉胡阿川\01lhjk\事业\AXS设计工作室\AXS_设计工作室客户需求深度调研表.html'
$html=Get-Content -LiteralPath $path -Encoding UTF8 -Raw
$scripts=[regex]::Matches($html,'(?s)<script>\s*(.*?)\s*</script>') | ForEach-Object { $_.Groups[1].Value }
$bundle=($scripts -join "`n")
$tmp=Join-Path $env:TEMP 'axs-survey-scripts.js'
Set-Content -LiteralPath $tmp -Value $bundle -Encoding UTF8
node --check $tmp
```

## 5. 工作流理解

AXS 系统的大流程是：

1. 客户通过 H5 表单提交结构化需求。
2. 系统将表单数据转为 JSON / Markdown 等可分析格式。
3. 本地解析中枢结合审美探测、户型图、设计理念库和标准工艺库，生成用户需求白皮书。
4. 进入设计执行阶段，生成效果图、AutoLISP/CAD 结构图、预算与方案说明。
5. 施工阶段按 SOP、现场图片和工艺标准进行验证。
6. 项目完成后把问题、返工、材料和工艺经验沉淀到知识库，反哺后续项目。

核心原则：

- 先理解客户真实生活，再谈风格标签。
- 实用、舒适、审美和系统化要同时成立。
- 不要把系统做成给普通客户看的技术炫耀页。
- 面向客户的文案应温暖、可信、专业；面向内部的文档可以更系统化和工程化。

## 6. 文案与设计风格约定

面向客户的 H5 页面：

- 避免攻击性、审判式、过度“极客化”的措辞。
- 避免让客户感觉被筛选、被否定或被系统强迫。
- 可以保留 AXS 的系统感，但表达要服务于专业信任。
- 模块名尽量温暖且权威，例如：
  - `居住基础`
  - `空间规划`
  - `归家动线`
  - `核心生活区`
  - `健康舒适`
  - `生活取向`
  - `美学愿景`
- 客户可见按钮不要出现内部工具名，例如 `Obsidian`。

内部系统文档：

- 可以保留 Agentic、SOP、RAG、AutoLISP、CAD、预算、验收、复盘等工程化表达。
- 如果文档中有过时的 SaaS、飞书、Obsidian 或过强军事化表达，修改前先确认用户想保留哪种语气。

## 7. 安全与文件操作边界

本项目用户明确要求：

- 禁止批量删除文件或目录。
- 不要使用：
  - `del /s`
  - `rd /s`
  - `rmdir /s`
  - `Remove-Item -Recurse`
  - `rm -rf`
- 需要删除文件时，只能一次删除一个明确路径的文件。
- 如果需要批量删除文件，应停止操作，并让用户手动删除。

额外约定：

- 修改前先确认真实目标文件，尤其是多个相似 HTML 版本并存时。
- 不要随意覆盖 `_最终完美版`、`_极客终极版` 等版本文件。
- 如果只是修复浏览器旧 URL 失效，可以从 `_最终完美版.html` 复制恢复入口文件，但要说明原因。
- 不要把临时脚本、字段 JSON、历史修复脚本当作垃圾文件清理。
- Windows PowerShell 下读中文 Markdown/HTML 优先使用 `-Encoding UTF8`。
- 中文路径复杂，命令中优先使用 `-LiteralPath`。

## 8. 推荐协作节奏

用户偏好：

- 复杂任务先对齐目标、边界和真实文件，再执行。
- 表单微调、文案替换、选项增删这类明确任务可以直接小步修改并验证。
- 每次改完应简短说明：
  - 改了哪个文件。
  - 改了哪一块。
  - 是否做了基本校验。

处理 H5 表单时推荐流程：

1. 先用 `Select-String` 或局部 `Get-Content` 定位目标模块。
2. 用 `apply_patch` 小块修改。
3. 检查关键词是否存在/旧文案是否消失。
4. 检查 JavaScript 语法。
5. 最后给用户可点击的绝对路径和行号。

## 9. 重要提醒

- 当前根目录本身是一个 Git 仓库，但历史上曾出现 `git diff` 在某些上下文不可用的情况；不要依赖 Git 状态作为唯一判断。
- in-app browser 对 `file://` 本地路径可能会报 `NAVIGATION FAILED WITH ERROR CODE -6`。若页面打不开，先检查文件是否存在，不要先判断为网络问题。
- 若当前浏览器 URL 指向 `AXS_设计工作室客户需求深度调研表.html`，这个入口文件必须存在。
- 若入口文件丢失，优先核对 `_最终完美版.html` 是否为最新定稿，再复制恢复。
